<%= react_component('ShowCharacter', {
	character: @character,
	campaigns: @campaigns,
	currentCampaign: @character.campaign,
  background: @character.background,
  powers: @character.powers,
  deleteable: @deleteable
}) %>
